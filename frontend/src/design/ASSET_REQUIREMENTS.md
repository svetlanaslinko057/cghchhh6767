# ASSET REQUIREMENTS (to be provided by the owner)

All temporary assets are composition-only areas (neutral surfaces). They MUST NOT depict a
fictional person or imply real data. Replace before production where marked [BLOCK].

## 1. Portrait of Oksana [BLOCK]
- Purpose: Scene 4 (Person) + small header/footer optional.
- Orientation: vertical (portrait), aspect ~4:5.
- Min resolution: 1600\u00d72000 px, sharp, well-lit, neutral/warm background matching paper palette.
- Framing: head-and-shoulders or environmental (at desk with documents). Keep safe margins: subject not touching edges; ~12% padding around head for crop flexibility.
- Formats: JPG/PNG (we convert to optimised WebP). Provide 1\u20132 options (close + wider).
- Until provided: neutral image-shell (paper-deep block with subtle inner shadow), tagged `data-temporary` in code, NO face, NO \u201cPlaceholder\u201d label.

## 2. Handwriting / manuscript sample [TEMP now \u2192 optional real later]
- Purpose: Scene 1 fragment + Scene 3 proof.
- v1 uses the fictional sample in content/uk.js (no personal data).
- If replaced with a real one: MUST be anonymised (no names, IDs, dates of real persons), owner consent required.
- Format: high-res scan JPG/PNG, min 2000px on long side, straightened, good contrast.

## 3. Document-type examples (/work) [TEMP]
- Purpose: inner page gallery of document categories.
- v1: stylised neutral document surfaces (typography + paper), no personal data.
- Real (optional): fully redacted/anonymised scans only.

## 4. Logo / monogram [REAL, generated]
- `OO` monogram + wordmark lockup \u2014 produced in-code (SVG/type), no external asset needed.

## 5. Contact data [BLOCK]
- Real email, phone, messengers (Telegram/WhatsApp/Signal), working hours, city/region, language of correspondence.
- Optional: legal/privacy text for footer.

## Delivery
Send files + text via the owner. Store real contacts ONLY in content/*.js contact object.
